type fancontrol, domain;
type fancontrol_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(fancontrol)

# Allow access to fan GPIO sysfs
allow fancontrol sysfs_gpio:file rw_file_perms;
allow fancontrol sysfs_thermal:file r_file_perms;
